apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  revisionHistoryLimit: 3
  replicas: 3
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend-container
          image: 479699168320.dkr.ecr.ap-northeast-2.amazonaws.com/chaing-backend:160
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 80
            periodSeconds: 20
            failureThreshold: 3
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret